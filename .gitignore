# dependencies
node_modules/
.pnpm-store/

# build output — electron-vite bundles into out/, electron-builder into dist*/release/
out/
dist/
dist-*/
release/

# agent scratch
.claude-octopus/

# typescript incremental / project-reference state
*.tsbuildinfo
.tsbuildinfo/

# secrets — never commit real provider keys.
# OpenCode stores credentials in its own auth store (see README), not here.
.env
.env.*
!.env.example
opencode.json
auth.json

# logs
*.log
npm-debug.log*
logs/

# editors / os
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# caches
.cache/
.vite/
.eslintcache
